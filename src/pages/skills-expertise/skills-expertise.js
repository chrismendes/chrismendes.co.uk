'use strict';

// (TODO: Try pathmodify via npm, instead of remapify)
// var tabs = require('components/tabs.js'); // (This doesn't work)
var tabs = require('../../components/tabs/tabs.js'); // (This does)
